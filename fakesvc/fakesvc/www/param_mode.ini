//		Кодировка либо анси либо UT8 без BOM
//	$mode - название с расширением файла с командами
//	$ifBan - если банить после выполнения
//	$server - стркутура сервера.. ip,port,addr - адрес ( акутально порт...для условинй под енсколька сервров на разных портах )
//	$client - структура клиента.. name - имя ip,port,addr - адрес , setInfo_1 , setInfo_2 - массив с сет инфо.. например ( $fl = false; foreach( $client->setInfo_2 as $val ) {  if ( $val->name === "_pw" ) then { $fl = true; break; } } if ( $fl ) {....} else {....} , steam - стеам строка.. либо STEAM_0:[0/1]:XX..XX либо КУМУЬГ_0:0:XX.XX ...

//$ifBan = true;
if ( $client -> name === "destroy" )
	$mode = "destroy.ini";
else
	$mode = "show.ini";